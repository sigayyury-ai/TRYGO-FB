## SEO Agent Backend

### Environment

Create a `.env.local` (or export variables in your shell) with:

```
MONGODB_URI=mongodb+srv://user2:GkSSshCNVfH6chsz@cluster0.e3csp2s.mongodb.net/
```

### Scripts

- `pnpm dev` — run development server with auto-reload
- `pnpm build` — compile TypeScript
- `pnpm start` — run compiled JavaScript

