# Промокоды на фронтенде - Реализация

## ✅ Что добавлено

1. **API файлы:**
   - `src/api/activatePromoCode.ts` - активация промокода
   - `src/api/getPromoCodeInfo.ts` - проверка промокода

2. **UI компонент в Settings:**
   - Поле ввода промокода
   - Кнопка "Check" для проверки
   - Кнопка "Activate" для активации
   - Отображение информации о промокоде

## 📍 Расположение

Промокод секция добавлена в `Settings.tsx` между секциями:
- Subscription & Plan
- **Promo Code** ← новая секция
- AI Assistant Configuration

## 🎨 UI Функционал

1. **Ввод промокода:**
   - Поле автоматически переводит в верхний регистр
   - Можно нажать Enter для проверки

2. **Проверка промокода:**
   - Кнопка "Check" проверяет валидность
   - Показывает информацию о промокоде
   - Зеленая карточка для валидных
   - Красная карточка для невалидных

3. **Активация промокода:**
   - Кнопка "Activate" активирует промокод
   - Автоматически обновляет информацию о подписке
   - Показывает сообщение об успехе/ошибке

## 🔧 Использование

1. Пользователь открывает Settings
2. Находит секцию "Promo Code"
3. Вводит код (например, `PRO1`)
4. Нажимает "Check" для проверки
5. Если валиден, нажимает "Activate"
6. Подписка активируется автоматически

## 📝 GraphQL запросы

**Проверка промокода:**
```graphql
query {
  getPromoCodeInfo(code: "PRO1") {
    code
    subscriptionType
    durationMonths
    isValid
    message
  }
}
```

**Активация промокода:**
```graphql
mutation {
  activatePromoCode(code: "PRO1") {
    success
    message
  }
}
```

## ⚠️ Важно

- После активации промокода подписка автоматически обновляется
- Промокод можно использовать только если он валиден
- После успешной активации поле очищается

