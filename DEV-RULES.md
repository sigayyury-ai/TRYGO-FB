# TRYGO SEO Agent - Правила работы

## 🚀 Запуск серверов

### Вариант 1: Всё сразу (рекомендуется)
```bash
./start-all.sh
```

**Автоматическая установка зависимостей**: Скрипт автоматически проверяет наличие `node_modules` и устанавливает зависимости через `npm install`, если они отсутствуют. Это предотвращает ошибки запуска после очистки `node_modules`.

### Вариант 2: По отдельности
```bash
# Backend
./start-backend.sh

# Frontend (в другом терминале)
./start-frontend.sh
```

### Остановка всех серверов
```bash
./stop-all.sh
```

---

## 📋 ПРАВИЛА ДЛЯ COPILOT

### ❌ НИКОГДА НЕ ДЕЛАТЬ:

1. **НЕ запускать серверы через `run_in_terminal` с `isBackground=true`**
   - Это убивает существующие процессы
   - Приводит к постоянным перезапускам

2. **НЕ делать curl запросы напрямую в терминал где запущен сервер**
   - Это прерывает процесс сервера

3. **НЕ использовать Ctrl+C в терминале с сервером**

### ✅ ПРАВИЛЬНЫЙ ПОДХОД:

1. **Проверка статуса серверов:**
   ```bash
   # Проверить порты БЕЗ остановки
   lsof -i:4100  # backend
   lsof -i:5173  # frontend
   ```

2. **Тестирование API:**
   ```bash
   # Используй НОВЫЙ терминал для curl
   curl http://localhost:4100/api/seo/dashboard?projectId=...
   ```

3. **Просмотр логов:**
   ```bash
   tail -f logs/backend.log
   tail -f logs/frontend.log
   ```

4. **Если нужен рестарт:**
   ```bash
   ./stop-all.sh
   ./start-all.sh
   ```

---

## 🔧 Полезные команды

### Проверить что работает
```bash
# Быстрая проверка портов
lsof -i:4100 -i:5173
```

### Посмотреть логи
```bash
# Backend логи
tail -f logs/backend.log

# Frontend логи  
tail -f logs/frontend.log

# Оба сразу
tail -f logs/*.log
```

### Очистить логи
```bash
rm logs/*.log
```

---

## 🎯 Workflow для разработки

1. **Старт проекта:**
   ```bash
   ./start-all.sh
   ```

2. **Разработка:**
   - Редактируй код
   - HMR автоматически обновит
   - Логи в `logs/`

3. **Тестирование API:**
   - Открой НОВЫЙ терминал
   - Делай curl запросы

4. **Завершение:**
   ```bash
   ./stop-all.sh
   ```

---

## 📝 Технические детали

- **Backend:** Express REST API на порту 4100
- **Frontend:** Vite React на порту 5173
- **Database:** MongoDB Atlas (база: test)
- **User:** sigayyury5@gmail.com
- **Project:** AI-Powered MVP Builder
- **Hypothesis:** Solo founders

---

## 🐛 Если что-то сломалось

```bash
# 1. Останови всё
./stop-all.sh

# 2. Убей процессы если остались
killall node

# 3. Очисти порты
lsof -ti:4100 | xargs kill -9
lsof -ti:5173 | xargs kill -9

# 4. Запусти заново
./start-all.sh
```
