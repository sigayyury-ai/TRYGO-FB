# Troubleshooting пустого экрана

## Анализ кода

### Логика Index.tsx

В `src/pages/Index.tsx` есть автоматический редирект:
- Если пользователь авторизован → редирект на `/dashboard`
- Если не авторизован → редирект на `/auth`

**Это нормально!** Пустой экран может быть временным во время редиректа.

## Что проверить в браузере

### 1. Откройте DevTools (F12 или Cmd+Option+I)

#### Console Tab
Проверьте на ошибки:
- ❌ Red errors - проблемы с загрузкой модулей
- ⚠️ Yellow warnings - предупреждения
- 🔴 React errors - ошибки в компонентах

**Типичные ошибки:**
- `Failed to fetch` - проблема с backend API
- `Cannot read property of undefined` - ошибка в коде
- `Module not found` - проблема с импортами

#### Network Tab
Проверьте:
- ✅ Загружается ли `main.tsx`? (статус 200)
- ✅ Загружаются ли другие модули?
- ✅ Есть ли GraphQL запросы? (к `/graphql`)
- ❌ Есть ли failed запросы? (красные строки)

#### Application Tab → Local Storage
Проверьте наличие:
- `useUserStore` - данные пользователя
- Если нет токена → должен быть редирект на `/auth`

### 2. Проверьте URL в адресной строке

После загрузки URL должен измениться на:
- `/auth` - если не авторизован
- `/dashboard` - если авторизован

Если URL остается `/` - возможно проблема с роутингом.

### 3. Проверьте что сервер отвечает

```bash
curl http://localhost:8080
```

Должен вернуть HTML с `<div id="root"></div>`

## Возможные проблемы и решения

### Проблема 1: Ошибки в Console

**Если видите ошибки в консоли:**
1. Скопируйте текст ошибки
2. Проверьте что все зависимости установлены: `npm install`
3. Проверьте что нет проблем с импортами

### Проблема 2: Backend недоступен

**Симптомы:**
- В Network видно failed запросы к `/graphql`
- Ошибки типа "Failed to fetch" или "Network error"

**Решение:**
- Backend находится на `https://ailaunchkit-backend-production.onrender.com/graphql`
- Проверьте что backend доступен
- Для локальной разработки может потребоваться локальный backend

### Проблема 3: React не рендерится

**Симптомы:**
- Нет ошибок в консоли
- HTML загружается, но `#root` пустой

**Проверьте:**
1. Откройте Elements tab в DevTools
2. Найдите `<div id="root"></div>`
3. Проверьте есть ли внутри что-то

**Решение:**
- Проверьте ErrorBoundary - возможно он поймал ошибку
- Проверьте что React компоненты импортируются правильно

### Проблема 4: Бесконечный редирект

**Симптомы:**
- URL меняется, но ничего не показывается
- В Network видно множественные запросы

**Возможная причина:**
- Проблема с проверкой авторизации
- Циклический редирект между `/` и `/auth`

## Что делать сейчас

### Шаг 1: Откройте браузер DevTools
1. Откройте http://localhost:8080
2. Нажмите F12 (или Cmd+Option+I на Mac)
3. Откройте вкладку **Console**

### Шаг 2: Проверьте ошибки
- Если видите красные ошибки → скопируйте и отправьте мне
- Если ошибок нет → переходите к шагу 3

### Шаг 3: Проверьте Network
1. Откройте вкладку **Network**
2. Обновите страницу (F5)
3. Проверьте что загружаются файлы (main.tsx, App.tsx и т.д.)

### Шаг 4: Проверьте URL
- После загрузки URL должен измениться
- Если остался `/` - проблема с роутингом

## Быстрая диагностика

Выполните в терминале:

```bash
# Проверить что сервер работает
curl -I http://localhost:8080

# Проверить логи процесса (если запущен через скрипт)
# Или посмотреть в терминале где запускали npm run dev
```

## Следующие шаги

После диагностики:
1. Если есть ошибки → отправьте текст ошибки
2. Если все загружается но пусто → проверим роутинг
3. Если редирект работает → это нормально, должно показывать `/auth` страницу

