# Чеклист проверки в браузере

## Почему может быть пустой экран

### Это нормально если:
1. ✅ **Редирект происходит** - страница `/` автоматически редиректит на `/auth` или `/dashboard`
2. ✅ **React загружается** - может быть небольшой момент белого экрана во время загрузки
3. ✅ **Нет ошибок в консоли** - если нет красных ошибок, все работает

### Это проблема если:
1. ❌ **Есть красные ошибки в Console** - JavaScript ошибки
2. ❌ **URL не меняется** - остается на `/` более 2-3 секунд
3. ❌ **В Network видны failed запросы** - красные строки с ошибками

---

## Пошаговая проверка

### Шаг 1: Откройте DevTools

1. Откройте http://localhost:8080
2. Нажмите **F12** (или **Cmd+Option+I** на Mac)
3. Откройте вкладку **Console**

### Шаг 2: Проверьте ошибки

**Что искать:**
- 🔴 **Красный текст** = ошибки
- 🟡 **Желтый текст** = предупреждения
- ⚪ **Белый/серый текст** = информационные сообщения

**Если видите ошибки:**
- Скопируйте текст ошибки
- Проверьте строку где ошибка (например, `App.tsx:42`)
- Наиболее частые ошибки:
  - `Cannot read property 'X' of undefined` - проблема с данными
  - `Module not found` - проблема с импортами
  - `Failed to fetch` - проблема с backend

### Шаг 3: Проверьте Network tab

1. Откройте вкладку **Network**
2. Обновите страницу (**F5** или **Cmd+R**)

**Что должно быть:**
- ✅ `main.tsx` - загружается (статус 200)
- ✅ Другие `.tsx` и `.ts` файлы - загружаются
- ✅ CSS файлы - загружаются
- ❌ **НЕТ** красных строк (failed requests)

**Если видите failed запросы:**
- Проверьте URL запроса
- Проверьте статус (например, 404, 500, CORS error)
- Запросы к `/graphql` могут fail если backend недоступен (это нормально для начала)

### Шаг 4: Проверьте URL

**После загрузки страницы:**
- URL должен измениться с `/` на `/auth` (если не авторизован)
- Или на `/dashboard` (если авторизован)

**Если URL меняется:**
- ✅ Это нормально! Приложение работает
- Страница `/auth` должна показаться после редиректа

**Если URL НЕ меняется:**
- ❌ Проблема с роутингом
- Проверьте ошибки в Console

### Шаг 5: Проверьте элементы страницы

1. Откройте вкладку **Elements** (или **Inspector**)
2. Найдите `<div id="root"></div>`
3. Разверните его (кликните на стрелку)

**Что должно быть:**
- Внутри `#root` должен быть React контент
- Если пусто → React не рендерится (проверьте ошибки)

---

## Ожидаемое поведение

### Сценарий 1: Первый запуск (не авторизован)

1. Открываете http://localhost:8080
2. Видите белый экран 0.5-1 секунду (React загружается)
3. URL меняется на `/auth`
4. Появляется страница авторизации с:
   - Формой входа/регистрации
   - Кнопкой Google Login

### Сценарий 2: Уже авторизован

1. Открываете http://localhost:8080
2. Видите белый экран 0.5-1 секунду
3. URL меняется на `/dashboard`
4. Появляется Dashboard с Sidebar

---

## Типичные проблемы

### Проблема: Белый экран долго (более 5 секунд)

**Возможные причины:**
1. Backend недоступен - приложение пытается подключиться
2. Ошибка в коде - проверьте Console
3. Проблема с импортами - проверьте Console

**Решение:**
- Проверьте Console на ошибки
- Проверьте Network на failed запросы
- Попробуйте обновить страницу (Ctrl+R / Cmd+R)

### Проблема: Ошибка "Failed to fetch" или "Network error"

**Это нормально если:**
- Backend на production не доступен
- Для локальной разработки нужен локальный backend

**Что делать:**
- Можно игнорировать если это только GraphQL запросы
- Страница авторизации должна все равно показаться
- Можно попробовать войти (но запрос может не пройти)

### Проблема: Ошибка в Console при загрузке

**Если видите ошибки:**
1. Скопируйте полный текст ошибки
2. Обратите внимание на файл и строку (например `App.tsx:42`)
3. Проверьте что все зависимости установлены: `npm install`

---

## Что мне нужно от вас

Для диагностики мне нужно знать:

1. **Что показывает Console?**
   - Есть ли красные ошибки? (если да - скопируйте текст)
   - Есть ли желтые предупреждения?

2. **Что показывает Network?**
   - Загружаются ли файлы? (main.tsx, App.tsx и т.д.)
   - Есть ли failed запросы? (красные строки)

3. **Меняется ли URL?**
   - Остается `/` или меняется на `/auth`/`/dashboard`?

4. **Что показывает Elements tab?**
   - Есть ли что-то внутри `<div id="root">`?

---

## Быстрая проверка

Выполните в консоли браузера (Console tab):

```javascript
// Проверка что React загрузился
console.log('React loaded:', typeof window.React !== 'undefined');

// Проверка что роутер работает
console.log('Current path:', window.location.pathname);

// Проверка stores (если доступны)
// В консоли браузера можете попробовать:
// localStorage.getItem('useUserStore')
```

---

## Если все еще пусто

Попробуйте:
1. **Жесткое обновление**: Ctrl+Shift+R (или Cmd+Shift+R на Mac)
2. **Очистить кеш браузера**
3. **Попробовать в другом браузере** (Chrome, Firefox, Safari)
4. **Проверить что порт свободен**: `lsof -ti:8080`

Если проблема сохраняется - отправьте:
- Скриншот Console с ошибками
- Скриншот Network tab
- Текст ошибок (если есть)






