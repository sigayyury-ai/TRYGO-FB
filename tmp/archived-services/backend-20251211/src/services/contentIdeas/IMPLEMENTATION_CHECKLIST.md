# Чеклист реализации: CJM и JTBD в генераторе идей

## ✅ Что уже реализовано

### Бэкенд
- ✅ **seoContext.ts** (строки 158-159): Загружает `customerJourney` и `jtbd` из `hypothesesPersonProfiles`
- ✅ **generator.ts**: Использует `icp?.customerJourney` и `icp?.jtbd` во всех категориях
- ✅ **Промпты расширены**: Все 7 категорий используют CJM и JTBD
- ✅ **Обработка отсутствующих данных**: Проверки на `null` и fallback значения

### Фронтенд
- ✅ **generateContentIdeas.ts**: Правильно вызывает мутацию с `projectId` и `hypothesisId`
- ✅ **SeoContentPanel.tsx**: Корректно передает параметры в генератор
- ✅ **Нет изменений требуется**: Фронтенд не должен знать о CJM/JTBD - это внутренняя логика бэкенда

---

## 🔍 Что нужно проверить

### 1. Структура данных в БД
**Проверить, что в коллекции `hypothesesPersonProfiles` есть поля:**
- `customerJourney` (string или object)
- `jtbd` (string)

**Как проверить:**
```javascript
// В MongoDB shell или через MongoDB Compass
db.hypothesesPersonProfiles.findOne({ 
  projectHypothesisId: ObjectId("your-hypothesis-id") 
})
```

**Если полей нет:**
- Нужно убедиться, что форма ICP позволяет заполнять эти поля
- Или добавить миграцию для заполнения существующих записей

### 2. Тестирование генератора
**Проверить генерацию для каждой категории:**

```bash
# Тест с данными, где есть CJM и JTBD
# Должно работать для всех категорий:
- PAIN
- GOAL  
- TRIGGER
- FEATURE
- BENEFIT
- FAQ
- INFO
```

**Проверить промпты:**
- Открыть логи бэкенда при генерации
- Убедиться, что в промпте есть секции с CJM и JTBD (если данные есть)
- Убедиться, что промпт не ломается, если данных нет

### 3. Проверка работы с пустыми данными
**Сценарии для тестирования:**
- ✅ ICP без `customerJourney` - генератор должен работать
- ✅ ICP без `jtbd` - генератор должен работать
- ✅ ICP без обоих полей - генератор должен работать
- ✅ ICP с обоими полями - генератор должен использовать их

---

## 🚨 Потенциальные проблемы

### Проблема 1: Поля не существуют в БД
**Симптомы:**
- Генератор работает, но не использует CJM/JTBD
- В промптах нет секций с CJM/JTBD

**Решение:**
- Проверить структуру `hypothesesPersonProfiles`
- Убедиться, что форма ICP позволяет заполнять эти поля
- Добавить миграцию, если нужно

### Проблема 2: Неправильные имена полей
**Симптомы:**
- Данные не загружаются из БД
- `icp.customerJourney` всегда `null`

**Решение:**
- Проверить, как именно называются поля в БД
- Возможно: `customer_journey`, `customerJourneyMap`, `journeyMap`
- Обновить `seoContext.ts` с правильными именами

### Проблема 3: Формат данных
**Симптомы:**
- Данные загружаются, но промпт выглядит странно
- CJM/JTBD отображаются как `[object Object]`

**Решение:**
- Проверить формат данных в БД (string vs object)
- Добавить сериализацию, если нужно:
  ```typescript
  customerJourney: typeof personProfile.customerJourney === 'string' 
    ? personProfile.customerJourney 
    : JSON.stringify(personProfile.customerJourney)
  ```

---

## 📝 Рекомендации

### 1. Добавить логирование
В `generator.ts` добавить логи, чтобы видеть, что передается:
```typescript
console.log(`[generator] CJM available: ${!!customerJourney}`);
console.log(`[generator] JTBD available: ${!!jtbd}`);
```

### 2. Добавить валидацию
Проверить, что данные корректны перед использованием:
```typescript
const customerJourney = icp?.customerJourney 
  ? (typeof icp.customerJourney === 'string' ? icp.customerJourney : JSON.stringify(icp.customerJourney))
  : "";
```

### 3. Тестирование
Создать тестовые сценарии:
- С полными данными ICP (включая CJM и JTBD)
- С частичными данными (без CJM или без JTBD)
- С минимальными данными (только persona)

---

## ✅ Итоговый статус

**Бэкенд:** ✅ Готов (код написан, нужно только проверить данные в БД)
**Фронтенд:** ✅ Готов (изменений не требуется)
**Генератор:** ✅ Готов (использует CJM и JTBD во всех категориях)

**Следующий шаг:** Проверить структуру данных в БД и протестировать генерацию






