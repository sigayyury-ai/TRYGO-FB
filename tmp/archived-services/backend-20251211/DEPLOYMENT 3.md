# 🚀 Инструкция по деплою SEO Agent на Render

## Быстрый старт

### 1. Подготовка

Убедитесь, что у вас есть:
- ✅ Аккаунт на Render (https://render.com)
- ✅ MongoDB база данных (MongoDB Atlas или Render MongoDB)
- ✅ OpenAI API ключ
- ✅ Репозиторий на GitHub/GitLab

### 2. Создание сервиса на Render

#### Вариант A: Через Render Dashboard (рекомендуется для первого раза)

1. Зайдите в Render Dashboard → **New** → **Web Service**
2. Подключите ваш репозиторий
3. Выберите репозиторий и ветку
4. **Важно**: Укажите **Root Directory** как `backend`
5. Render автоматически определит настройки из `render.yaml`

#### Вариант B: Через render.yaml (автоматически)

Если файл `render.yaml` находится в корне репозитория:
1. Render автоматически создаст сервис при первом деплое
2. Все настройки будут взяты из `render.yaml`

### 3. Настройка переменных окружения

В Render Dashboard перейдите в **Environment** и добавьте:

#### Обязательные переменные:

```bash
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/database
OPENAI_API_KEY=sk-your-openai-api-key-here
FRONTEND_URL=https://your-frontend-url.com
```

#### Опциональные переменные (имеют значения по умолчанию):

```bash
NODE_ENV=production
OPENAI_MODEL=gpt-4o-mini
OPENAI_IMAGE_MODEL=dall-e-2
SEMANTICS_SERVICE_URL=http://localhost:4200
IMAGES_SERVICE_URL=http://localhost:4300
WEBSITE_PAGES_SERVICE_URL=http://localhost:4400
IMAGE_PROVIDER=openai
```

#### WordPress интеграция (опционально):

```bash
WORDPRESS_BASE_URL=https://your-wordpress-site.com
WORDPRESS_USERNAME=your-username
WORDPRESS_APP_PASSWORD=your-app-password
```

### 4. Первый билд

1. Нажмите **Create Web Service** или **Save Changes**
2. Render начнет процесс билда:
   - Установит зависимости (`npm install`)
   - Соберет проект (`npm run build`)
   - Запустит сервис (`npm start`)
3. Следите за логами в реальном времени
4. Дождитесь сообщения "Your service is live"

### 5. Проверка деплоя

После успешного деплоя проверьте:

1. **Health endpoint**: 
   ```
   https://your-service-name.onrender.com/
   ```
   Должен вернуть JSON с информацией о сервисе

2. **GraphQL endpoint**:
   ```
   https://your-service-name.onrender.com/graphql
   ```
   Должен открыть GraphQL Playground (в development) или вернуть ошибку 405 (в production)

3. **Логи**: Проверьте логи в Render Dashboard на наличие ошибок

## 🔧 Настройка MongoDB

### MongoDB Atlas

1. Создайте кластер на https://www.mongodb.com/cloud/atlas
2. Создайте пользователя базы данных
3. Добавьте IP адреса Render в whitelist:
   - В MongoDB Atlas → Network Access → Add IP Address
   - Добавьте `0.0.0.0/0` для всех IP (или конкретные IP Render)
4. Скопируйте connection string и используйте как `MONGODB_URI`

### Render MongoDB

1. В Render Dashboard → **New** → **MongoDB**
2. Выберите план и регион
3. После создания скопируйте **Internal Database URL** или **External Connection String**
4. Используйте как `MONGODB_URI`

## ⚠️ Решение проблем

### Билд падает

**Проблема**: `npm install` или `npm run build` завершается с ошибкой

**Решение**:
- Проверьте, что `package.json` и `package-lock.json` закоммичены
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте версию Node.js (Render использует последнюю LTS)

### Сервис не запускается

**Проблема**: Билд успешен, но сервис не отвечает

**Решение**:
- Проверьте логи в Render Dashboard
- Убедитесь, что `MONGODB_URI` установлен правильно
- Проверьте, что сервер слушает порт из переменной `PORT` (Render устанавливает автоматически)

### Ошибки подключения к MongoDB

**Проблема**: `MongooseError: connect ECONNREFUSED` или `MongoNetworkError`

**Решение**:
- Проверьте `MONGODB_URI` формат
- Убедитесь, что MongoDB whitelist включает IP адреса Render
- Проверьте username/password в connection string

### CORS ошибки

**Проблема**: Frontend не может подключиться к API

**Решение**:
- Установите `FRONTEND_URL` на правильный URL вашего фронтенда
- Убедитесь, что URL не заканчивается на `/`
- Проверьте, что CORS настроен в коде (уже настроен в `src/index.ts`)

## 📝 Чеклист перед деплоем

- [ ] Код закоммичен и запушен в репозиторий
- [ ] `render.yaml` создан и находится в папке `backend/`
- [ ] `package.json` содержит все необходимые зависимости
- [ ] `package-lock.json` закоммичен
- [ ] MongoDB база данных создана и доступна
- [ ] OpenAI API ключ получен
- [ ] Все переменные окружения подготовлены
- [ ] Frontend URL известен (для CORS)

## 🔄 Обновление деплоя

После каждого пуша в основную ветку Render автоматически:
1. Обнаружит изменения
2. Запустит новый билд
3. Развернет новую версию (zero-downtime deployment)

Вы также можете запустить manual deploy из Dashboard.

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Проверьте статус сервиса
3. Убедитесь, что все переменные окружения установлены
4. Проверьте документацию Render: https://render.com/docs

